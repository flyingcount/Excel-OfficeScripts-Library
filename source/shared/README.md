# Shared helpers

Copy these functions into a script file. The Excel Automate editor runs a **single file**; `import` / `export` will not load this folder.

Keep helpers small and free of `main`. If a helper grows a feature-specific branch, move that logic into the script instead.
